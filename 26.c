#include <stdio.h>
void main()
{
  int a, b, j, temp, number, rem;
  printf("Enter two numbers(intervals): ");
  scanf("%d %d", &a, &b);
  printf("Armstrong numbers between %d an %d are: ", a, b);
  for(j=n1+1; j<n2; ++j)
  {
      temp=;
      number=0;
      while(temp!=0)
      {
          rem=(temp%10);
          num+=rem*rem*rem;
          temp/=10;
      }
      if(j==number)
      {
          printf("%d ",j);
      }
  }
  
}
