#include <stdio.h>
int main()
{
int num;
printf("Enter a number \n");
scanf("%d", &num);
if (num >= 0)
{
printf("%d is a positive number \n", number);
}
else
{
printf("%d is a negative number \n", number);
}
return 0;
}
