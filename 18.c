#include <stdio.h>
int main()
{
int lower, higher, j, flag;
printf("Enter two numbers(intervals): ");
scanf("%d %d", &lower, &higher);
printf("Prime numbers between %d and %d are: ", lower, higher);
while (lower < higher)
{
flag = 0;
for(j = 2; j <= low/2; ++j)
{
if(low % j == 0)
{
flag = 1;
break;
}
return 0;
}
